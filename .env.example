# AWS Credentials
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_DEFAULT_REGION=us-east-1

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# Application Settings
UPDATE_INTERVAL=300  # Time in seconds between price updates
CACHE_EXPIRY=600    # Time in seconds before cache entries expire
LOG_LEVEL=INFO      # Logging level (DEBUG, INFO, WARNING, ERROR)

# Instance Types to Monitor (comma-separated)
INSTANCE_TYPES=t2.micro,t3.micro,t3a.micro

# API Settings
API_PORT=5001
API_HOST=0.0.0.0
